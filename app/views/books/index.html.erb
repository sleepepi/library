<h1 style="color:blue;">Listing books</h1>

<table>
  <thead>
    <tr>
      <th style="font-family:arial;color:green;">Name</th>
      <th style="font-family:arial;color:green;">Author</th>
      <th style="font-family:arial;color:green;">Borrower</th>
      <th style="font-family:arial;color:green;">Available</th>
      <th style="font-family:arial;color:green;">Checkout date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <style>
        td
        {
          border-collapse:collapse;
          border:1px solid black;
        }
        td
        {
          padding:10px;
        }
      </style>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.name %></td>
        <td><%= book.author %></td>
        <td><%= book.borrower %></td>
        <td><%= book.available %></td>
        <td><%= book.checkout_date %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path, class: 'btn btn-primary' %>
